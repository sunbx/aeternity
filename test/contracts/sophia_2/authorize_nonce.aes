contract GA =
  record state = { nonce : int, checks : int }

  function init() = { nonce = 1, checks = 0 }

  stateful function nonce_correct(n : int) : bool =
    if(state.nonce != n) abort("Incorrect nonce")
    put(state{nonce = n + 1})
    true

